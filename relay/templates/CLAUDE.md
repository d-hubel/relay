@workflow/instructions.md

<!-- The line above loads the project rules automatically at the start of every
     Claude Code session and again after compaction. Keep it on its own line,
     never inside backticks or a code block, or it stops loading.

     Add rules that only Claude Code needs below this comment (commands to run,
     git habits). Rules that chat must also follow belong in
     workflow/instructions.md. Don't contradict instructions.md here; change the
     rule there instead. -->
